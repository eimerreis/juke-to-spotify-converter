'use strict';
var request = require('request');
var GlobalConfig = require('../../conf/config');
var SpotifyConfig = require('spotify-config');

function SpotifyProvider(){
}

SpotifyProvider.prototype.connect = function(){
  var requestOptions = {
      url: SpotifyConfig.
  }
};